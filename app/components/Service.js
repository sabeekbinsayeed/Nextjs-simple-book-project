import React from 'react';

const Service = (element) => {
    const { id, body, title } = element;
    return (
        <div>
            <h1>{title}</h1>
            <input></input>

        </div>
    );
};

export default Service;